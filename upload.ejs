<div class="postcontainer mt-5 flex flex-wrap">
      <% user.posts.reverse().forEach(function(post){ %>
          <div class="post w-full md:w-1/2 lg:w-1/3 p-4 border border-gray-300 m-2 rounded-md bg-white shadow-sm">
              <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
              <p class="text-sm tracking-tight text-gray-700"><%= post.image %></p>
          </div>            
     <% }) %>
  </div>

  <!-- app.post('/homepage',isloggedin,async function(req,res){
    let user = await bookmodel.findOne({email: req.user.email});
    let {image} = req.body;
    let post = await bookmodel.create({
        author : author._id,
        image,
    })

    user.posts.push(post._id);
    await user.save()  
    res.render("homepage",{user,image:post.image});
}); -->